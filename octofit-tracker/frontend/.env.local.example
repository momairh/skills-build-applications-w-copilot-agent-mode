# Required so the frontend can build API URLs for the forwarded Codespaces port.
# Copy this file to .env.local and set it to the actual Codespace name
# (usually available as $CODESPACE_NAME in the Codespaces terminal).
VITE_CODESPACE_NAME=
